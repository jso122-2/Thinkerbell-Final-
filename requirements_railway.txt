# Ultra-Minimal Railway Requirements - Fastest Build
# Only absolutely essential dependencies

# Web framework (minimal)
fastapi==0.104.1
uvicorn==0.24.0
pydantic==2.5.0

# Essential utilities
requests==2.31.0
python-dotenv==1.0.0
pyyaml==6.0.1

# Lightweight ML dependencies (CPU-only for smaller image)
torch==2.1.0+cpu
transformers==4.35.0
sentence-transformers==2.2.2
numpy==1.24.3
